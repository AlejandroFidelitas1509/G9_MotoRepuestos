@model IEnumerable<G9MotoRepuestos.Models.PuntoVenta>

@{
    ViewData["Title"] = "Punto de Venta -  Moto Repuestos Rojas";
}


<h2>Punto de Venta</h2>


<a name=""
   id=""
   class="btn btn-primary"
   asp-action="Crear"
   role="button">Crear</a>

<div class="mt-3">
   <table class="table">
    <thead>
        <tr>
            <th>Codigo</th>
            <th>Descripcion</th>
            <th>Cantidad</th>
            <th>Total</th>
            <th>Acciones</th>
        </tr>
    </thead>
        @* <tbody>
            @foreach (PuntoVenta itemDato in Model)
            {

                <tr class="">

                    <td>@itemDato.Id</td>
                    <td>@itemDato.Nombre</td>
                    <td>@itemDato.Descripcion</td>
                    <td>@itemDato.Precio</td>
                    <td>@itemDato.Cantidad</td>
                    <td>
                        <a name=""
                           id=""
                           class="btn btn-primary"
                           asp-action="Editar"
                           asp-route-id="@itemDato.Id"
                           role="button">Editar</a>

                        <a name=""
                           id=""
                           class="btn btn-danger"
                           asp-action="Eliminar"
                           asp-route-id="@itemDato.Id"
                           role="button">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody> *@
    </table>

   </div>