@{
    ViewData["Title"] = "Cierres Contables";
}

<div class="container">
    <h2 class="titulo">Cierres Contables</h2>

    <!-- Selector del tipo de cierre -->
    <div class="card">
        <label class="label">Tipo de cierre</label>
        <select id="tipoCierre" class="input" onchange="cambiarFormulario()">
            <option value="diario">Diario</option>
            <option value="semanal">Semanal</option>
            <option value="mensual">Mensual</option>
        </select>
    </div>

    <!-- FORMULARIO DIARIO -->
    <form method="get" asp-action="CierreResultado" asp-controller="Contabilidad" id="formDiario" class="card">
        <h3 class="subtitulo">Cierre Diario</h3>

        <label class="label">Fecha</label>
        <input type="date" name="fecha" class="input" required />

        <input type="hidden" name="tipo" value="diario" />

        <button class="btn">Realizar cierre diario</button>
    </form>

    <!-- FORMULARIO SEMANAL -->
    <form method="get" asp-action="CierreResultado" asp-controller="Contabilidad" id="formSemanal" class="card" style="display:none;">
        <h3 class="subtitulo">Cierre Semanal</h3>

        <label class="label">Fecha inicio</label>
        <input type="date" name="fechaInicio" class="input" required />

        <label class="label">Fecha fin</label>
        <input type="date" name="fechaFin" class="input" required />

        <input type="hidden" name="tipo" value="semanal" />

        <button class="btn">Realizar cierre semanal</button>
    </form>

    <!-- FORMULARIO MENSUAL -->
    <form method="get" asp-action="CierreResultado" asp-controller="Contabilidad" id="formMensual" class="card" style="display:none;">
        <h3 class="subtitulo">Cierre Mensual</h3>

        <label class="label">Mes</label>
        <input type="month" name="fechaInicio" class="input" required />

        <input type="hidden" name="tipo" value="mensual" />

        <button class="btn">Realizar cierre mensual</button>
    </form>
</div>

<!-- SCRIPT PARA MOSTRAR/OCULTAR -->
<script>
    function cambiarFormulario() {
        var tipo = document.getElementById("tipoCierre").value;

        document.getElementById("formDiario").style.display = tipo === "diario" ? "block" : "none";
        document.getElementById("formSemanal").style.display = tipo === "semanal" ? "block" : "none";
        document.getElementById("formMensual").style.display = tipo === "mensual" ? "block" : "none";
    }
</script>

<!-- ESTILOS INTERNOS (sin archivo CSS) -->
<style>
    .container {
        max-width: 700px;
        margin: 30px auto;
        background: #ffffff;
        padding: 25px;
        border-radius: 15px;
        box-shadow: 0 0 10px rgba(0,0,0,0.10);
    }

    .titulo {
        text-align: center;
        font-size: 28px;
        margin-bottom: 25px;
        color: #2c3e50;
        font-weight: bold;
    }

    .subtitulo {
        font-size: 20px;
        margin-bottom: 15px;
        color: #3b4b5a;
        font-weight: 600;
    }

    .card {
        background: #f7f9fc;
        padding: 20px;
        border-radius: 12px;
        margin-bottom: 25px;
        border: 1px solid #d5dce3;
    }

    .label {
        font-weight: 600;
        color: #3b4b5a;
        display: block;
        margin-bottom: 5px;
    }

    .input {
        width: 100%;
        padding: 10px;
        border-radius: 8px;
        border: 1px solid #bfc7d1;
        background: #ffffff;
        margin-bottom: 15px;
        font-size: 15px;
    }

    .btn {
        width: 100%;
        background: #5c9ded;
        color: white;
        padding: 12px;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        transition: .2s;
        font-size: 16px;
        font-weight: 600;
    }

        .btn:hover {
            background: #4a89d8;
        }
</style>
